Фильтр ```title``` возвращает сроку в которой все слова (токены), если они начинаются на букву, преобразовываются к виду в котором первый символ строчный:

```twig
{{ 'my super world'|title }}
{# выведет: My Super World #}
```
